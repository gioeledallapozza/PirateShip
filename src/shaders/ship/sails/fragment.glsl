varying vec2 vUv;

void main() {
    // Visualizziamo le UV come colori
    // Se la vela diventa rossa a sinistra e gialla a destra, 
    // allora uv.x cresce da sinistra a destra.

    // csm_DiffuseColor = vec4(vUv, 0.0, 1.0);
}